{
  "name": "homebridge-powerwall-meters",
  "version": "0.4.2",
  "description": "Powerwall meters, battery status and controls over HomeKit HAP using pypowerwall",
  "main": "src/index.js",
  "license": "MIT",
  "keywords": [
    "homebridge-plugin",
    "powerwall",
    "energy"
  ],
  "engines": {
    "node": "^22 || ^24 || ^26",
    "homebridge": ">=2.4.0 <3"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/client.js && node --check src/meters.js && node --check src/hap.js"
  },
  "files": [
    "src",
    "config.schema.json",
    "README.md",
    "docs",
    "LICENSE"
  ],
  "devDependencies": {
    "homebridge": "2.4.0"
  }
}
