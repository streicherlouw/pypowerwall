{
  "name": "homebridge-powerwall-meters",
  "version": "0.5.0",
  "description": "Grouped Powerwall Matter accessory with energy, battery sensors and controls",
  "main": "src/index.js",
  "license": "MIT",
  "keywords": [
    "homebridge-plugin",
    "powerwall",
    "energy",
    "supports-matter"
  ],
  "engines": {
    "node": "^22 || ^24 || ^26",
    "homebridge": ">=2.4.0 <3"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check src/client.js && node --check src/meters.js && node --check src/matter.js"
  },
  "files": [
    "src",
    "config.schema.json",
    "README.md",
    "docs",
    "LICENSE"
  ],
  "devDependencies": {
    "homebridge": "2.4.0"
  }
}
